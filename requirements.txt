Flask==3.0.3
pandas==2.2.3
openpyxl==3.1.5
requests==2.32.3
pypdf==5.9.0
python-docx==1.2.0

# Requis uniquement en production, quand DATABASE_URL pointe vers PostgreSQL.
# Optionnel en développement local (SQLite par défaut, aucune config requise).
psycopg2-binary==2.9.9

# Charge automatiquement le fichier .env au démarrage (pratique en local).
python-dotenv==1.0.1

# Requis uniquement en production, pour la facturation Stripe.
# Optionnel en développement local (billing désactivé sans ces clés).
stripe==10.12.0

# Génération des rapports PDF mensuels téléchargeables.
fpdf2==2.7.9

gunicorn==22.0.0
waitress==3.0.0
pytest==8.3.5

# Lecture sécurisée des anciens fichiers Excel .xls.
xlrd==2.0.1

# Email transactionnel via Resend.
resend==2.37.0

# Rate limiting distribué (Render Key Value / Valkey, compatible Redis).
Flask-Limiter[redis]==4.1.1
