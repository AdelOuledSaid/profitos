services:
  - type: web
    name: profitos
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/cloud_preflight.py --init-tenants && gunicorn --workers 1 --threads 4 --timeout 120 --bind 0.0.0.0:$PORT wsgi:app
    healthCheckPath: /readyz
    autoDeployTrigger: commit
    envVars:
      - key: PROFITOS_ENV
        value: production
      - key: PROFITOS_SECRET_KEY
        generateValue: true
      - key: APP_BASE_URL
        value: https://profitos.onrender.com
      - key: MAX_UPLOAD_MB
        value: 15
      - key: SESSION_HOURS
        value: 12
      - key: DATABASE_URL
        fromDatabase:
          name: profitos-db
          property: connectionString

databases:
  - name: profitos-db
    databaseName: profitos
    user: profitos
    plan: free
    region: frankfurt
